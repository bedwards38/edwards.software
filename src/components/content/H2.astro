---
const { class: className, ...rest } = Astro.props;
const html = await Astro.slots.render("default");
const customClass = `${className ?? ""} typewriter cursor`;
---

<h2 {...rest} class={customClass} data-content={html}></h2>
